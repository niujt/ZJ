<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wxthxy.zj.dao.ApplicationQuestionDAO" >
    <select id="getCount" resultType="Integer">
        select count(1) from ApplicationQuestion;
    </select>
    <select id="getAllApplicationQuestion" resultType="com.wxthxy.zj.entity.Question">
        select qt.type as type,qt.level as level,aq.*
        from ApplicationQuestion aq left join questiontype qt
        on aq.questiontypeid=qt.id;
    </select>
    <insert id="addApplicationQuestion" parameterType="com.wxthxy.zj.entity.Question">
        insert into ApplicationQuestion(questiontypeid,text,answer)
        values(#{questiontypeid},#{text},#{answer})
    </insert>
</mapper>